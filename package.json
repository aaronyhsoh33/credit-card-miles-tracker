{
  "name": "credit-card-miles-tracker",
  "private": true,
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "api": "npm run dev --workspace=api",
    "app": "npm run start --workspace=app",
    "dev": "concurrently \"npm run api\" \"npm run app\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
